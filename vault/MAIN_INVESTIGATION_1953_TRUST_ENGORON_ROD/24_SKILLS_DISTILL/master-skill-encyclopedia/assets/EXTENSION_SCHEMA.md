# Extension Schema
Required JSON fields: id, name, domain_id, domain, category, trigger, inputs, outputs, execution_rules, version, guided_link(optional).
Rules: unique id; lawful only; prefer guided_link to specialized skills; bump version on bulk add; run validate_masters.py.
