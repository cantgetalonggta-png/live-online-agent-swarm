# RPT-004 — DocumentCloud 25547032 + IA epsteindocs pass
**HITL=YES · public-record · dual-persist**

## 1. Operator question answered: what still needs explicit HITL?
See locked matrix: `00_HITL_AND_INDEX/HITL_AUTHORITY_MATRIX.md` (H1–H15).

### Standing autonomous under HITL=YES (A1–A10)
Public search; single public PDF/HTML; targeted OCR; graph/BC/FU/RS; dual-persist; starter Q20/5BC/5 methods; best-lead rank+exec; FU close; jmail.world; local swarm.

### Explicit re-auth still required
| ID | Needs HITL phrase |
|---|---|
| H1 | Bulk harvest >20 files or >500 MB one host → `HITL=YES bulk [source]` |
| H2 | Full multi-hour OCR campaign whole multiparts → `HITL=YES full OCR [range]` |
| H3 | New Drive folder / large multi-GB upload post-quota → `HITL=YES Drive push` |
| H4 | Git force-push / history rewrite / secret scrub → `HITL=YES git force/scrub` |
| H5 | New investigation scope outside map → `HITL=YES new scope` |
| H6 | Elevate CONTESTED motive to ROCK_SOLID without M5 primary → `HITL=YES tag upgrade` |
| H7 | Non-public / login / paywalled / credentials → **NEVER** |
| H8 | Publish outside operator GitHub+Drive → `HITL=YES publish` |
| H9 | 30-min deep pass new unnamed person → `HITL=YES 30min [name]` |
| H10 | PACER paid → `HITL=YES PACER` |
| H11 | Email/contact living third party → **ALWAYS ask** |
| H12 | Amend permanent method files → `HITL=YES amend method` |
| H13 | Promote operator theory notes to evidence → `HITL=YES promote theory` |
| H14 | GDPR bulk personal-data beyond need → `HITL=YES GDPR note` |
| H15 | Public internet swarm deploy → `HITL=YES public deploy` |

### Commands that auto-gate
`bulk download epsteindocs/*` · `OCR all SQUADS Parts` · Drive push after quota fail · `git push --force` · investigate new person as primary · `mark ROCK_SOLID` motive without primary · login/API key · email counsel · amend METHOD_* · public deploy · promote MY THEORIES · PACER buy · anything non-public.

## 2. This pass findings (authorized URLs)
- **DocumentCloud PDF held** (224 pp). Letter p1 = Bondi→Patel demand (RS-012 ROCK_SOLID). p224 = redacted list tail 241–254.
- **Control-triangle names: NULL** in extractable text of this pack.
- **IA epsteindocs** indexed (561 files / ~5 GB). Full mirror still **H1**. Priority titles catalogued; black book + Wexner + plea/bail/flight already on priority path.

## 3. Map impact
- Adds process/oversight edge: DOJ AG ↔ FBI Director (Feb 2025 transparency order).
- Does **not** move 1953 Trust / Indyke–Kahn–Southern / Engoron rod edges.
- Reinforces that public “Epstein files” dumps may be flight/contact/victim-redaction heavy — money/control primaries still in EFTA / SQUADS / estate dockets / jmail.

## 4. Next ranked leads (autonomous rules)
1. jmail.world people search Indyke / Kahn / Southern / trust / wire (A9)  
2. Targeted OCR only on SQUADS E pages that hit trust/wire keywords (A3, not H2)  
3. Cross-ref black-book Indyke entry (RS-011) to estate docket trustee filings  
4. Do **not** bulk-mirror full IA without H1  

## 5. Dual-persist
Local vault written. GitHub + Drive on next push cycle.
